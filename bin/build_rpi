#!/bin/bash

export CC=arm-linux-gnueabihf-gcc
export CXX=arm-linux-gnueabihf-g++
export CGO_ENABLED=1
export GOOS=linux
export GOARCH=arm
export GOARM=7

BUILD_DIR=build
rm -rf ${BUILD_DIR}
mkdir -p ${BUILD_DIR}

find cmd -name *.go | xargs -I{} go build -o ${BUILD_DIR} {}
